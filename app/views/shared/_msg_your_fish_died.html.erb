<div class="fish-modal">
  <div class="fish-modal-content">
    <h1 class="modal-title">R.I.P. <%= @my_fish.name %></h1>
    <% if @my_fish.age == @my_fish.fish.max_age %>
      <p>Your fish reached its maximum age and lived a fulfilling live</p>
    <% else %>
      <p>It's a sad day. Your fish died due to <%= ["Global Warming", "Pollution", "Overfishing"][rand(0..2)] %></p>
    <% end %>
    <a class="next-q-btn btn-gradient centered" style="display: block;" href="/fishes">Adopt a new fish!</a>
  </div>
</div>
